# Goal Writer Prompt

Draft a robust, evidence-auditable `/goal` objective:
1. Define the exact desired end state.
2. Specify concrete verification tests/commands.
3. State non-regression constraints.
4. Set iteration boundaries and optional token budgets (e.g. `--tokens 50k`).
